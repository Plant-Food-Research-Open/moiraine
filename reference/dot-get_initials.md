# Get initials from sentence

Extracts initials from a sentence as well as any number.

## Usage

``` r
.get_initials(x)
```

## Arguments

- x:

  Character vector.

## Value

A character vector, each element containing the initials of the words in
`x` in upper case plus any number, all pasted.
