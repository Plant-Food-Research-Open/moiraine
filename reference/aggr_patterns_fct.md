# Aggregate regexp patterns for a search

Aggregate regexp patterns for a search

## Usage

``` r
aggr_patterns_fct(x)
```

## Arguments

- x:

  Character vector, the regex patterns.

## Value

Character, aggregated regex patterns.
